//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file, except  programmer edit sections, will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { useEffect } from 'react';
import { isAxiosError } from 'axios';
import { useQuery } from '@tanstack/react-query';

import { AggregationClassService } from '@/services/AggregationClass.service';
import type { IBaseAggregationClass } from '@/types/AggregationClass.types';
import type { GetRequestOptions } from '@/types/helpers/GetRequestOptions';

const useGetAggregationClass = <T extends IBaseAggregationClass>({
  id,
  viewName,
  enabled = true,
  onNotFound,
}: GetRequestOptions) => {
  const { data, isLoading, isError, error } = useQuery({
    queryKey: ['aggregationClass', id],
    retry: false,
    enabled: enabled,
    queryFn: async (): Promise<T> => {
      const response = await AggregationClassService.GetAggregationClass(id, viewName);
      return response.data;
    },
  });

  useEffect(() => {
    if (isError && isAxiosError(error)) {
      if (typeof onNotFound === 'function' && error.response?.status === 404) {
        onNotFound();
      }
    }
  }, [error, isError, onNotFound]);

  return {
    data,
    isLoading,
    isError,
  };
};

export default useGetAggregationClass;
