//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file, except  programmer edit sections, will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { useMutation, useQueryClient } from '@tanstack/react-query';
import { AxiosError } from 'axios';

import { BusinessClassService } from '@/services/BusinessClass.service';

const useDeleteAllBusinessClass = (onSuccess: (result: string[]) => void, onError: (e: Error | AxiosError) => void) => {
  const queryClient = useQueryClient();

  const {
    mutate: deleteAllBusinessClass,
    mutateAsync: deleteAllBusinessClassAsync,
    isPending,
  } = useMutation({
    mutationFn: (ids: string[]) => BusinessClassService.DeleteAllBusinessClass(ids),
    onSuccess: (_, ids) => {
      const className = 'businessClass';
      ids.forEach((id) => {
        queryClient.removeQueries({ queryKey: [className, id] });
      });
      queryClient.invalidateQueries({ queryKey: [className, 'list'] });
      queryClient.invalidateQueries({ queryKey: [className, 'count'] });
      onSuccess(ids);
    },
    onError: (e) => onError(e),
  });

  return { deleteAllBusinessClass, deleteAllBusinessClassAsync, isPending };
};

export default useDeleteAllBusinessClass;
