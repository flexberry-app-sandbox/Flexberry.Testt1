//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file, except  programmer edit sections, will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { useMutation, useQueryClient } from '@tanstack/react-query';
import { AxiosError } from 'axios';

import { BusinessClassService } from '@/services/BusinessClass.service';
import { IBaseBusinessClass } from '@/types/BusinessClass.types';

const useCreateBusinessClass = <T extends IBaseBusinessClass>(
  onSuccess: (result: T) => void,
  onError: (e: Error | AxiosError) => void
) => {
  const queryClient = useQueryClient();

  type CreateRecordArgs = {
    businessClass: T;
    viewName: string;
  };

  const {
    mutate: createBusinessClass,
    mutateAsync: createBusinessClassAsync,
    isPending,
  } = useMutation({
    mutationFn: (createRecordArgs: CreateRecordArgs) =>
      BusinessClassService.CreateBusinessClass<T>(createRecordArgs.businessClass, createRecordArgs.viewName),
    onSuccess: (response, variables) => {
      const id = response.data.id;
      const { viewName } = variables;
      const className = 'businessClass';
      queryClient.invalidateQueries({ queryKey: [className, id] });
      queryClient.invalidateQueries({ queryKey: [className, 'list', viewName] });
      queryClient.invalidateQueries({ queryKey: [className, 'count'] });
      onSuccess(response.data);
    },
    onError: (e) => onError(e),
  });

  return { createBusinessClass, createBusinessClassAsync, isPending };
};

export default useCreateBusinessClass;
